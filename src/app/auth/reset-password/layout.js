import React from "react";
export const metadata = {
  title: "Reset Password - LMS",
  description: "AI based Customer Detection System",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
